---
interface Props {
  text: string;
}
const { text } = Astro.props;
---

<div class="mb-5 flex items-center gap-3 text-sm font-semibold uppercase tracking-[0.16em] text-[#00a7e1]">
  <span class="size-3 bg-[#c8d600]"></span>
  <span>{text}</span>
</div>